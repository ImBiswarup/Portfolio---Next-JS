/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.pexels.com', 'lh3.googleusercontent.com', 'res.cloudinary.com', 'asset.cloudinary.com'],
    },
};

export default nextConfig;
